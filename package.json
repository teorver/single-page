{
  "name": "tms_course_project",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build src/index.html --no-source-maps --no-cache --public-url .",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "latest",
    "gh-pages": "^6.3.0",
    "parcel": "latest"
  },
  "dependencies": {
    "swiper": "^10.3.1"
  }
}
